## Agent skills

### Issue tracker

本地 Markdown，issue 存放在 `.scratch/` 目录下。 See `docs/agents/issue-tracker.md`.

### Triage labels

五个标准分诊角色使用默认标签名。 See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — 根目录 `CONTEXT.md` + `docs/adr/`。 See `docs/agents/domain.md`.
