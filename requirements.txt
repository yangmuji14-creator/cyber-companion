# ===========================================================
# 赛博伴侣 — 依赖清单
# 安装：pip install -r requirements.txt
# 或用：python install.py（自动创建虚拟环境 + 国内镜像）
# ===========================================================

# ── 核心依赖（必须） ──
python-dotenv>=1.1.0
pydantic>=2.10.0
litellm>=1.65.0
loguru>=0.7.0
numpy>=1.24.0
aiohttp>=3.9.0

# ── 向量记忆（可选 — 缺失时自动降级为关键词搜索） ──
sentence-transformers>=3.0.0,<5.0.0

# ── 微信适配器（可选 — 仅使用微信时需要） ──
# weixin-ilink>=0.3.5
# qrcode>=7.0

# ── 开发依赖（测试用，普通用户可忽略） ──
# 安装方式：pip install -r requirements-dev.txt
