# Core
python-dotenv>=1.1.0
pydantic>=2.10.0

# LLM
litellm>=1.65.0

# Utilities
loguru>=0.7.0

# Vector Memory (optional — app falls back to keyword search if unavailable)
sentence-transformers>=3.0.0
